name: Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4g -Dorg.gradle.daemon=false -Dkotlin.incremental=false"

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v3

      - name: Configure JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 11

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v3

      - name: Run lint
        run: ./gradlew spotlessCheck

  offline-checks:
    name: Offline Checks
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v3

      - name: Configure JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 11

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v3

      - name: Run offline checks
        run: >
          ./gradlew
          :openai-core:jvmTest
          :openai-core:jsTest
          :openai-core:wasmJsTest
          :openai-core:apiCheck
          :openai-client:apiCheck
          :openai-client:compileTestKotlinJvm
          :openai-client:compileTestKotlinJs
          :openai-client:compileTestKotlinWasmJs

  live-smoke-jvm:
    name: Live Smoke JVM
    runs-on: ubuntu-latest
    needs: offline-checks
    if: ${{ github.repository == 'aallam/openai-kotlin' && secrets.OPENAI_API_KEY != '' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v3

      - name: Configure JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 11

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v3

      - name: Run live smoke tests
        run: >
          ./gradlew
          :openai-client:jvmTest
          --tests "*.TestModels"
          --tests "*.TestEmbeddings.embeddings"
          --tests "*.TestChatCompletions.chatCompletions"
        env:
          OPENAI_LIVE_TESTS: "1"
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

  publish-docs:
    runs-on: ubuntu-latest
    needs: [ offline-checks, live-smoke-jvm ]
    if: ${{ github.repository == 'aallam/openai-kotlin' && github.ref == 'refs/heads/main' && (needs['live-smoke-jvm'].result == 'success' || needs['live-smoke-jvm'].result == 'skipped') }}
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v3

      - name: Configure JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 11

      - name: Setup Gradle
        uses: gradle/gradle-build-action@v3

      - name: Build docs
        run: ./gradlew dokkaHtmlMultiModule

      - name: Deploy docs
        uses: JamesIves/github-pages-deploy-action@releases/v4
        with:
          branch: gh-pages
          folder: docs
          single-commit: true
